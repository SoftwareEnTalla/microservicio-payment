Provider Adapters
=================

This folder will contain provider adapter implementations (Stripe, PayPal, TropiPay, etc.).

Each adapter should implement a common interface (createPayment, authorize, capture, refund, status).
